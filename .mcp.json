{
  "_comment": "Claude Code MCP server registration. design-system-server-runner.js wraps mcp/design-system-server.json (design_search / design_system_generate / design_stack_search) and delegates to vendor/ui-ux-pro-max/scripts/search.py (Python3). Kept for UI design work; whether the new harness uses it is decided in the ui-loop design.",
  "mcpServers": {
    "vais-design-system": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/design-system-server-runner.js"]
    }
  }
}
