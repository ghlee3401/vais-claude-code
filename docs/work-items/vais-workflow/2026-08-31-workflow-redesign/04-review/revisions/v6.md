---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 4
status: blocked
based_on:
  - ../01-plan/main.md
  - ../02-design/main.md
  - ../03-do/main.md
verdict: BLOCKED
reviewed_at: 2026-09-03
qa_attempt: 6
---

# Review Attempt 6 — Design revision 4

## 판정

**BLOCKED — 공정 비교 adapter와 공통 품질 observer의 보완·회귀 검증은 PASS지만, 첫 formal cohort가 evidence adapter 결함으로 중단되어 유효한 Opus 3×2 표본이 없다.** EFF-01~08 평가, 최종 승인과 Report는 진행하지 않는다.

| 범위 | 기대 | 실제 | 판정 | Evidence |
|---|---|---|---|---|
| REQ-001~006 | 다섯 Phase·세 승인·Review 후 Report | 상태·승인·freeze 회귀 유지 | PASS | 전체 회귀 |
| REQ-007~012 | Feature/Work item 관계·단일 진행·pending | index·lease·pending 회귀 유지 | PASS | runtime focused |
| REQ-013~020 | 규모·문서 깊이·Agent·scope·Readiness | preflight, assignment, scope, 자동 Do→Review 통과 | PASS | 전체 회귀 |
| REQ-021~029 | 독립 QA·repair·evidence·snapshot·draft | 기존 공격 방어와 evidence 결속 회귀 유지 | PASS | 전체 회귀·focused tests |
| REQ-030 | 동일 조건·고정 순서의 신뢰 가능한 Opus 3×2 비교 | adapter·runner·observer는 PASS; 첫 cohort는 Legacy 1 뒤 중단 | **BLOCKED** | formal attempt·observer recheck |

## Formal Attempt 결과

고정 순서의 첫 실행인 `Legacy 1`은 전체 5-turn 여정을 완료했다. terminal은 completed였고 QA 문서와 실제 Chrome 검증은 PASS였지만, 최초 adapter가 Legacy native string phase 상태에 별도 `qaVerdict`가 없다는 이유로 quality fail로 오판했다. 이는 제품 품질 실패가 아니라 evidence adapter 결함이므로 Design revision 4 규칙에 따라 `v2 1` 시작 직후 cohort를 중단했다.

| Legacy 1 관측 | 값 |
|---|---:|
| terminal | completed |
| formal user turns | 5 |
| cache-creation tokens | 162,880 |
| elapsed | 601,728 ms |
| provider list cost | $3.3819068 |
| observed Agent calls | 1 |
| 공통 Chrome observer 재검사 | 5/5 PASS |

`v2 1`은 provider capture가 확정되기 전에 중단되어 표본으로 사용하지 않는다. 중단 전 비용은 raw JSON이 없어 관측 불가이며 0으로 간주하지 않는다. 중단된 실행의 원시 경로 `/tmp/vais-rev4-formal.zcH15H`와 observer 재검사는 진단 자료일 뿐 새 formal cohort에 포함하지 않는다.

## 보완·검증

- Legacy/v2 양쪽에 같은 신뢰된 외부 Chrome observer를 적용하고 initial, invalid-login, password-reset, login-book, book-cancel 5개 시나리오를 terminal quality에 결속했다.
- 전체 회귀: 569개 중 566 PASS, 환경성 3 SKIP, FAIL 0.
- Revision 4 focused: 25/25 PASS.
- 무과금 dry-run: paidCalls 0, 6개 byte-identical project seed와 `L1→v2-1→L2→v2-2→L3→v2-3` 순서 확인.
- ESLint, source validator, dependency audit, legacy path guard와 diff check 모두 PASS.
- 보완 observer로 보존된 Legacy 1을 재검사한 결과 5/5 PASS였으나, 중단된 cohort를 formal PASS로 승격하지 않았다.

## 제한과 다음 Gate

현재 유효한 paired 3×2 cohort가 없으므로 Quality 최종 판정과 EFF-01~08 수치를 만들 수 없다. 사용자가 **보완된 adapter로 새 cohort를 처음부터 실행**하도록 명시 승인해야 한다. 승인 시 새 immutable stage·새 cohort ID·새 격리 프로젝트를 만들고, 기존 결과를 대체 repetition으로 섞지 않은 채 고정 순서를 다시 실행한다.
