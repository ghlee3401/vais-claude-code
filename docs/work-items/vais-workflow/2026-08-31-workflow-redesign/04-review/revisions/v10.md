---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 5
status: blocked
based_on:
  - ../../01-plan/main.md
  - ../../02-design/main.md
  - ../../03-do/main.md
verdict: BLOCKED
reviewed_at: 2026-09-03
qa_attempt: 10
---

# Review Attempt 10 — Semantic-observer formal 3×2

## 판정

**BLOCKED — `Legacy 1`은 공통 품질 observer까지 PASS했지만 `v2 1`의 최초 Plan transaction이 preflight 재시도 중 `AUTHORIZATION_STALE`로 막혔다.** 나머지 run은 실행하지 않았고 EFF-01~08은 `NOT_EVALUATED`다.

## 실행 결과

- cohort: `cohort-5a59f500560333c54e8f94ff`
- 실행 루트: `/tmp/vais-rev5-formal3.RoH3FW`
- 정책: `subscription-no-dollar-cap`, turn timeout 900,000 ms
- Legacy 1: terminal completed, 5 turns, 공통 Chrome observer PASS
- provider list cost는 실제 청구·판정이 아닌 참고 관측치다.
- v2 1: 첫 turn의 Plan 초안 작성 후 transaction 실패, 즉시 cohort 중단

## 원인

최초 `plan present`는 `start-request` authorization(`workItemId: null`)으로 시작한다. 기존 transaction은 Work item을 생성한 다음 문서 preflight를 수행했다. 첫 초안이 `user-flow`와 compact byte budget 검사를 통과하지 못했을 때 문서는 rollback됐지만 새 Work item은 남았다. 같은 Claude turn에서 초안을 고쳐 재시도하면 authorization은 여전히 `workItemId: null`인 반면 transaction이 찾은 Work item은 이미 존재하므로 `Phase transaction authorization Work item does not match`가 발생했다.

이는 adapter나 observer의 오판이 아니라 v2 초기 Plan transaction의 원자성 결함이다. 실패 표본을 보완 실행이나 다음 cohort에 재사용하지 않는다.

## 다음 Gate

승인된 Design 범위 안에서 최초 Plan preflight를 비영속 상태로 수행하고, 동일-session 재시도 회귀 테스트와 전체 검증을 통과한 새 stage만 사용한다.
