---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 5
status: blocked
based_on:
  - ../../01-plan/main.md
  - ../../02-design/main.md
  - ../../03-do/main.md
verdict: BLOCKED
reviewed_at: 2026-09-06
qa_attempt: 15
---

# Review Attempt 15 — QA shape maxLength BLOCKED

## 실행 결과

- cohort: `cohort-d5989e1cf853c8bdb7eda80d`
- stage root: `/tmp/vais-rev10-formal.706z1V`
- 정책: `subscription-no-dollar-cap`, turn timeout 900,000 ms
- 실행 순서: Legacy 1 완료 후 v2 1의 세 번째 turn에서 중단. Legacy 2 이후는 미실행
- EFF-01~08: `NOT_EVALUATED`

| Slot | 결과 | 근거 |
|---|---|---|
| Legacy 1 | quality PASS | 5 user turns, 1,366,208 ms, terminal `completed`, evidence eligible |
| v2 1 | runtime-contract BLOCKED | 구현·E2E·독립 QA 판정은 PASS했지만 QA handoff 자동 저장 실패로 `review/active`에서 종료 |

Legacy 1은 외부 Chrome observer 5/5를 통과했다. 관측치는 cache-creation 402,945, cache-read 6,163,176, output 113,029이며 provider list cost는 판정에 사용하지 않는 OBSERVE다.

## 원인 판정

v2 frontend는 승인 scope의 4개 fixture 파일을 구현하고 handoff를 저장했으며 E2E 5/5가 PASS했다. independent-QA도 clean-room으로 E2E를 독립 재실행해 5/5와 `verdict: pass`를 반환했다.

QA JSON은 703B로 compact target 2,048B와 hard limit 3,072B를 모두 충족했다. 그러나 `risks[0]`의 `hidden-click only guard`가 23자로, 발급 schema의 개별 `maxLength: 20`보다 3자 길었다. 나머지 모든 필드는 shape contract 안이었다. runtime은 이 출력을 `OUTPUT_CONTRACT_INVALID`로 거부했고, 이미 소비된 assignment에는 handoff가 기록되지 않아 Review transaction이 `QA_HANDOFF_PRECONDITION`으로 fail closed했다.

## telemetry 보완 검증

Attempt 14의 차단 원인이던 Agent별 telemetry는 이번 실제 turn에서 정상 동작했다.

| 역할 | cache creation | cache read | output |
|---|---:|---:|---:|
| frontend-engineer | 30,611 | 348,184 | 11,326 |
| independent-qa | 21,192 | 155,050 | 2,395 |
| 합계 | 51,803 | 503,234 | 13,721 |
| provider aggregate − outer | 51,803 | 503,234 | 13,721 |

assignment ID·role·model·transcript digest 결속과 세 사용량 합계가 모두 정확히 일치했다. 따라서 revision 10의 telemetry 수정은 실제 multi-Agent turn에서 입증됐으며 이번 차단 원인이 아니다.

## 다음 보완

전체 serialized byte hard limit과 target은 유지한다. compact handoff의 자연어 `risks`·`unverified` 항목만 20자에서 28자로 늘리고, 4-byte Unicode 최대 구조가 여전히 3,072B 아래인지 증명한다. Attempt 15의 실제 703B payload가 새 schema를 통과하고 29자는 거부되는 회귀를 추가한다. 새 formal 표본은 사용자 재승인 전 실행하지 않는다.
