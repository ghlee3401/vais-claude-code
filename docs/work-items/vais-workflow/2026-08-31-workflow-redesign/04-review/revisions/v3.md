---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 1
status: failed
based_on:
  - ../01-plan/main.md
  - ../02-design/main.md
  - ../03-do/main.md
verdict: FAIL
reviewed_at: 2026-09-01
qa_attempt: 3
---

# Review Attempt 3 — archived

## Attempt 3 진행 상태

**FAIL — 품질은 통과했지만 실사용 효율 Gate를 통과하지 못했다.** 실제 `claude-opus-5`로 Legacy와 최신 v2의 동일 Mini Booking 전체 여정을 각각 3회 완료했다. v2는 3/3 `report/completed`·`reportFrozen: true`, 실제 Chrome 5/5, 독립 QA PASS, 누락·중복 Agent 0건이지만 instruction token과 문서 byte 중앙값이 Legacy보다 크다.

| 증거 | 결과 |
|---|---|
| paired full journey | Legacy/v2 3쌍 모두 terminal completed·quality PASS |
| v2 상태 머신 | 3/3 `report/completed`, final approved, Report frozen |
| 실제 화면 | 세 v2 실행 모두 Chrome 5/5와 390x844 취소 화면 증거 생성 |
| Agent 계획 | frontend/test/independent QA 3개를 각 1회 완료, 중복 0 |
| source 회귀 | 490개 중 487 PASS, 환경성 3 SKIP, 0 FAIL; lint·plugin validator·audit·schema·diff PASS |
| instruction token 중앙값 | Legacy `90,757` → v2 `362,446` — 효율 FAIL |
| authored Markdown 중앙값 | Legacy `6` → v2 `5` — 개선 |
| Markdown byte 중앙값 | Legacy `34,909` → v2 `39,268` — 효율 FAIL |
| Shadow verdict | `fail`; live-host evidence available, eligible 3/3, quality PASS, efficiency FAIL |

조건부 합성 승인 문구가 3/3 `clarify-final`로 차단된 뒤, 격리 평가의 명확한 승인 turn만 canonical final approval로 처리됐다. 공격 테스트 turn은 instruction token 합계에서 제외했지만 Claude Max 사용량 제한 이후의 Do 재개 비용은 실제 여정 비용으로 포함했다. 재개 turn 전체를 제거한 보수적 하한에서도 v2 중앙값은 약 `230,432`로 Legacy보다 크므로 판정은 바뀌지 않는다.

합의된 자동 QA 수정 상한 3회에 도달했다. 사용자 결정 없이 Repair 4의 Design/Do를 시작하지 않으며, Shadow PASS 전에는 source Report 작성·실제 최종 승인 요청·활성화를 금지한다.

## Attempt 2 판정

**BLOCKED.** QA repair 1 이후 clean-room Independent QA가 기존 일곱 결함과 추가 공격 경로를 재현했고, 최신 구현에서는 모두 차단됨을 확인했다. 구현 품질이나 안전 우회로 인한 FAIL은 남아 있지 않다. 다만 REQ-030/TC-021이 요구하는 실제 Claude Code의 Legacy/v2 전체 여정 3회 paired A/B가 없어 외부 검증 Gate를 통과할 수 없다.

## Attempt 2 구현 검증

| 항목 | 결과 |
|---|---|
| 기존 QA-01~07 재현 | 최신 runtime에서 차단 또는 정직한 BLOCKED로 처리 |
| 임의 schema-valid Gate/check 위조 | canonical recompute 및 Tool receipt 불일치로 거부 |
| stale authorization·lease | 현재 Work item/phase/status/scope 재검증으로 거부 |
| shell·Design scope·root write 우회 | 사전 차단 및 보호 namespace 분리 |
| drift routing 실패·최초 snapshot 공백 | baseline 보존 및 Gate snapshot으로 차단 |
| Review/Report 정본·Feature/Master | 전체 CLI 여정에서 completed/frozen 상태와 인덱스 일치 |
| 전체 테스트 | 474개 중 471 PASS, 3 SKIP, 0 FAIL |
| 정적·보안 검사 | lint, plugin/doc validator, secret scan, dependency audit PASS |
| 실제 UI | Mini Booking Chrome 데스크톱·모바일 4/4 PASS |

## 차단 증거

- `evidence/checks/shadow-evaluation.json`: `live-host-comparison` unavailable, 전체 verdict `blocked`
- `evidence/checks/live-ab-pilot.json`: 실제 Plan-only 1쌍이며 formal verdict `BLOCKED`
- 정식 PASS에는 동일 Mini Booking 전체 여정의 Legacy/v2 paired run을 repetition 1·2·3으로 측정하고 양쪽 quality PASS 및 v2의 instruction/Agent/document 감소를 증명해야 한다.

유료 live-host 실행 또는 평가 기준 변경 없이 이 증거를 만들었다고 간주하지 않는다. 따라서 사용자 최종 승인 요청과 Report 작성을 시작하지 않는다.

## Attempt 1 판정

**FAIL.** 단위·회귀·브라우저 검사는 통과했지만 실제 runtime 경로에서 안전 불변 조건을 우회하거나 증명하지 못하는 중대 결함이 발견됐다. 사용자 최종 승인을 요청하지 않고 Design checkpoint로 돌아간다.

## 독립 QA 발견 사항

| ID | 관련 요구사항 | 발견 |
|---|---|---|
| QA-01 | REQ-004/005/010/020/022 | binary Gate 결과 없이 readiness·QA·Report event를 직접 적용할 수 있고, 최초 Design 승인 없이 checkpoint event로 Do에 진입 가능 |
| QA-02 | REQ-006/019/021/025/029 | read-only로 시작하는 복합 Bash, redirect, `find -exec`로 write guard 우회 가능; Bash 사후 drift 확인도 없음 |
| QA-03 | REQ-004/005 | 승인 문구의 대상과 현재 Gate가 달라도 현재 Phase 승인을 적용함 |
| QA-04 | REQ-011/012 | 진행 중인 다른 요청을 runtime이 pending으로 자동 보관하지 않음 |
| QA-05 | REQ-003/017/018/021 | Independent QA에 implementation/code-write assignment 발급 가능 |
| QA-06 | REQ-029 | snapshot과 실제 repo drift detector가 없음 |
| QA-07 | REQ-030 | Shadow 비교가 실제 동일 실행이 아니며 하드코딩·비대칭 proxy를 PASS 근거로 사용함 |

## 통과한 보조 증거

- 전체 테스트 455개 중 452 PASS, 환경성 3 SKIP, FAIL 0
- lint, plugin/doc validator, dependency audit, secret scan PASS
- Mini Booking 실제 Chrome 4/4 동작과 데스크톱·모바일 화면 확인
- Legacy 기본값 유지와 문서 수준 Report freeze 단위 테스트 통과

## 다음 경로

QA repair 1로 기록하고 `Design → Do → readiness → Review`를 다시 수행한다. 위 일곱 항목의 runtime 증거가 없으면 PASS로 전환하지 않는다.
