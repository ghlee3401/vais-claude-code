---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 5
status: blocked
based_on:
  - ../01-plan/main.md
  - ../02-design/main.md
  - ../03-do/main.md
verdict: BLOCKED
reviewed_at: 2026-09-03
qa_attempt: 9
---

# Review Attempt 9 — Formal 3×2 cohort

## 판정

**BLOCKED — `Legacy 1`의 제품·Chrome 결과는 PASS였지만 공통 observer가 취소 시나리오의 이름과 manifest 개수를 과도하게 강제해 false FAIL을 만들었다.** `v2 1` 시작 직후 중단했고, 나머지 run은 실행하지 않았다. EFF-01~08은 `NOT_EVALUATED`다.

## 실행 결과

- cohort: `cohort-e0c7f3e8e22b9244e9ce5068`
- 실행 루트: `/tmp/vais-rev5-formal2.WKMMJO`
- 정책: `subscription-no-dollar-cap`, turn timeout 900,000 ms
- Legacy 1: terminal completed, 5 turns, 426,839 cache-creation tokens, 959,384 ms, Agent 6회
- provider list cost $9.35112885는 실제 청구·판정이 아닌 참고 관측치다.
- v2 1은 첫 turn 도중 중단되어 formal capture로 사용하지 않는다.

## False FAIL 원인

Legacy 결과에는 실제 취소 기능, `cancel-booking` Chrome 시나리오와 추가 `multi-class-booking` 커버리지가 있었고 자체 실행은 6/6 PASS였다. 기존 observer는 다음 두 가지를 동시에 강제했다.

1. 취소 시나리오 ID가 정확히 `book-cancel`이어야 한다.
2. manifest가 네 baseline과 취소 한 건, 정확히 다섯 시나리오만 가져야 한다.

Canonical workload는 시나리오 이름이나 추가 테스트 금지를 요구하지 않는다. 따라서 이는 유효한 제품 품질 실패가 아니라 evidence observer의 문자열·개수 계약 결함이다. 최초 확인 직후의 `FAIL` 언급은 raw evidence 대조 후 `BLOCKED`로 정정했다.

## 보완과 진단 재검증

- baseline 네 건은 기존 ID와 expected text를 그대로 잠근다.
- ID와 무관하게 취소 expected text를 가진 시나리오를 선택해 canonical `book-cancel` 역할로 정규화한다.
- 추가 manifest 시나리오의 존재는 거부 사유로 사용하지 않는다.
- 보존된 Legacy 1을 수정 observer로 진단 재검사한 결과 canonical Chrome 5/5 PASS다.
- focused 13/13, lint와 diff check PASS.

진단 재검사는 중단된 cohort를 formal PASS로 승격하거나 Legacy 1을 다음 cohort에 재사용하지 않는다.

## 다음 Gate

보완 observer로 새 immutable stage·새 nonce·새 project/session의 formal 3×2를 다시 실행하려면 사용자 명시 승인이 필요하다.
