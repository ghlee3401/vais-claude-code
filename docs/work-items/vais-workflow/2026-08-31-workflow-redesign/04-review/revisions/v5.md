---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 3
status: blocked
based_on:
  - ../01-plan/main.md
  - ../02-design/main.md
  - ../03-do/main.md
verdict: BLOCKED
reviewed_at: 2026-09-02
qa_attempt: 5
---

# Review Attempt 5 — Design revision 3

## 판정

**BLOCKED — 구현과 formal 실행 준비는 독립 QA PASS지만 실제 Legacy/v2 Opus 3×2 cohort가 없다.** Report, 사용자 최종 승인 요청과 v2 기본 활성화는 금지한다.

| 범위 | 기대 | 실제 | 판정 | Evidence |
|---|---|---|---|---|
| REQ-001~006 | 다섯 Phase·세 승인·Review 후 Report | 상태·승인·freeze 회귀 유지 | PASS | 전체 회귀 |
| REQ-007~012 | Feature/Work item 관계·단일 진행·pending | index·lease·pending 회귀 유지 | PASS | runtime focused |
| REQ-013~020 | 규모·문서 깊이·Agent·scope·Readiness | preflight, assignment, scope, 자동 Do→Review 통과 | PASS | `independent-qa-attempt5.json` |
| REQ-021~029 | 독립 QA·repair·evidence·snapshot·draft | 중복 check, 원자성, symlink와 stale 공격 차단 | PASS | `independent-qa-attempt5.json` |
| REQ-030 | 신뢰 가능한 동일 조건 3×2 효율 비교 | aggregator와 v2 runner는 준비; Legacy adapter와 실제 표본 부재 | **BLOCKED** | trusted aggregator tests |

## 요구사항·테스트 추적

검토 범위는 REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, REQ-008, REQ-009, REQ-010, REQ-011, REQ-012, REQ-013, REQ-014, REQ-015, REQ-016, REQ-017, REQ-018, REQ-019, REQ-020, REQ-021, REQ-022, REQ-023, REQ-024, REQ-025, REQ-026, REQ-027, REQ-028, REQ-029, REQ-030 전체다.

실행한 설계 테스트 범위는 TC-001, TC-014, TC-015, TC-016, TC-017, TC-018, TC-019, TC-020, TC-021, TC-022다. TC-014의 delivery/assurance 두 하위 사례도 이 추적으로 함께 기록한다.

## 입력·출력과 엣지·제한

- 입력: 승인된 Plan/Design revision 3, 실제 코드 diff, runtime receipt, stage와 tool profile digest, 독립 QA 공격 probe.
- 출력: 구현 및 formal 실행 준비 `PASS`, 실제 효율 비교 `BLOCKED`, Report 미생성.
- 엣지 케이스: 외부 symlink raw, 빈 capture, 서로 다른 Work item에 분산된 phase main, 증거 폴더의 `main.md`, 중복 supplemental check, 동시 check claim과 CLI option injection을 포함했다.
- 제한: Legacy adapter와 실제 Opus 3×2 표본이 없으므로 Quality·EFF 최종 출력은 만들 수 없다.

## 구현·QA 결과

- 전체 회귀: 564개 중 561 PASS, 환경성 3 SKIP, FAIL 0.
- ESLint, source validator, dependency audit, legacy path guard, diff check PASS.
- v2-only stage: 411 files, symlink 0, recursive digest와 isolated runtime require PASS; validator error 0, README 안내 warning 1.
- Independent QA는 외부 raw symlink, canonical 문서 누락·분산·오분류, 빈 capture, stage/profile self-attestation, CLI argv injection과 과거 check event 오염 공격을 재현한 뒤 최신 코드에서 모두 차단됨을 확인했다.
- 실제 유료 Claude/Opus 호출은 이번 attempt에서 하지 않았다.

## 차단 사유

1. Legacy 실행 결과를 `legacy-journey-evidence/v1`로 변환하는 신뢰 가능한 adapter가 아직 없다.
2. 승인된 Design §3.2는 양쪽에 같은 사용자 prompt를 요구하지만 공개 진입 계약이 다르다. Legacy는 `/vais cto plan {feature}`처럼 phase를 지정하고 v2는 `/vais {자연어 요청}`으로 시작하므로 한 문자열을 그대로 쓰면 한쪽이 정상 라우팅되지 않는다.
3. 이 차이를 숨긴 adapter를 만들거나 과거 v1 자료를 새 cohort로 위장하면 공정한 효율 검증이 아니다.

## 다음 Gate

사용자와 prompt 동등성 기준을 먼저 확정한다. 권장안은 **문자열 동일성 대신 사용자 의도·요구사항·fixture·모델·effort·tool profile은 동일하게 유지하고, 각 버전의 공개 진입 문법만 adapter가 번역**하는 것이다. 이는 승인된 측정 계약의 의미 변경이므로 Design revision 4로 기록하고 승인받은 뒤 Legacy adapter를 구현해 Opus 3×2를 실행한다.
