---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 5
status: blocked
based_on:
  - ../../01-plan/main.md
  - ../../02-design/main.md
  - ../../03-do/revisions/v11.md
verdict: BLOCKED
reviewed_at: 2026-09-10
qa_attempt: 17
---

# Review Attempt 17 — mutable stage isolation BLOCKED

## 실행 결과

- cohort: `cohort-3f9c08c0978a2cb890630359`
- stage root: `/tmp/vais-rev11-attempt17.o3YPf1`
- 정책: `subscription-no-dollar-cap`, turn timeout 900,000 ms
- 실행 순서: Legacy 1의 다섯 native user turn까지 실행 후 종료 무결성 검사에서 중단
- Legacy 1 formal eligibility: `false`; `journey-summary.json` 미생성
- v2 1, Legacy 2, v2 2, Legacy 3, v2 3: `NOT_RUN`
- EFF-01~08: `NOT_EVALUATED`
- durable evidence: `../evidence/attempt-17-blocked.json`

Legacy 1의 raw turn 다섯 개는 모두 provider success를 반환했고 작업 프로젝트에는 Report와 Chrome 5-scenario 산출물이 생성됐다. 그러나 공통 품질 observer와 trusted summary보다 먼저 실행되는 stage 종료 무결성 검사가 실패했으므로, 이를 품질 PASS나 정식 표본으로 소급 인정하지 않는다.

## 차단 원인

승인된 Legacy stage는 실행 전 SHA-256 `a20a4f48f14f68ff20cdf4ffcf3d83b706c4af16ac2cee210215f052940f177f`, 546 files였다. Legacy QA가 stage root의 `scripts/auto-judge.js`를 그 디렉터리에서 실행하면서 상대 경로 `.vais/event-log.jsonl` 254B를 생성했다. 실행 후 digest는 `9f2af9746ce13d71aab713f450031aab5f69aac63b54749e93a3dd0040445985`, 547 files가 됐다. 기존 546개 파일의 변경·삭제는 없고 추가 파일 한 개와 빈 `docs/booking-cancellation/03-do`, `04-qa` 디렉터리만 확인됐다.

이는 제품 구현의 품질 실패가 아니라 formal evaluator가 “immutable stage”를 쓰기 가능한 디렉터리로 제공한 격리 결함이다. 다만 Design revision 4의 fail-fast 규칙상 환경·증거 차이가 발생한 cohort는 즉시 `BLOCKED`이며 완료하지 않은 slot을 대체 실행하지 않는다.

## 다음 행동

Do revision 12에서 양쪽 stage를 실행 동안 읽기 전용으로 봉인하고 모든 종료 경로에서 원래 mode를 복원하도록 수정했다. 회귀·OS write probe·새 stage 무결성·무과금 dry-run은 모두 PASS다. 새 Attempt 18은 사용자 명시 승인 후 새 cohort ID로 처음부터 실행하며 Attempt 17의 raw turn은 재사용하지 않는다.
