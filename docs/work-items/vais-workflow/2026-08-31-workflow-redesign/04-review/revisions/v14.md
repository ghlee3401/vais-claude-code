---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 5
status: blocked
based_on:
  - ../../01-plan/main.md
  - ../../02-design/main.md
  - ../../03-do/main.md
verdict: BLOCKED
reviewed_at: 2026-09-06
qa_attempt: 14
---

# Review Attempt 14 — Combined Agent telemetry attribution BLOCKED

## 실행 결과

- cohort: `cohort-893af04cc2bb0c37a5def578`
- stage root: `/tmp/vais-rev9-formal.yBZ8DR`
- 정책: `subscription-no-dollar-cap`, turn timeout 900,000 ms
- 실행 순서: Legacy 1 완료 후 v2 1 collector에서 중단. Legacy 2 이후는 미실행
- EFF-01~08: `NOT_EVALUATED`

| Slot | 결과 | 근거 |
|---|---|---|
| Legacy 1 | quality PASS | 5 user turns, 1,333,182 ms, terminal `completed`, evidence eligible |
| v2 1 | evidence BLOCKED | 4 user turns과 제품 여정은 `report/completed`, QA PASS, report frozen이지만 formal telemetry 생성 전 중단 |

## 원인 판정

제품 구현과 QA shape-contract는 정상 동작했다. frontend handoff와 E2E 5/5가 PASS했고 independent-QA handoff는 1,650B로 2,048B target과 3,072B hard limit을 모두 충족했다. Work item도 최종 승인과 Report 동결까지 완료됐다.

차단 원인은 evaluator가 Design 승인 turn 하나에 frontend-engineer와 independent-qa가 함께 실행되는 정상 v2 compact 흐름을 지원하지 못한 것이다.

1. provider raw는 `subagent_stats.spawned: 2`, runtime manifest는 frontend 1회 + independent-QA 1회를 정확히 기록했다.
2. 기존 collector는 aggregate에서 top-level owner만 빼 나머지 전체를 QA assurance로 보는 방식이어서 delivery specialist가 같은 turn에 있으면 정확히 분리할 수 없었다.
3. 이를 숨기지 않기 위해 `buildAgentManifestEvidence`가 QA를 유일한 required Agent로 강제했고, 정상 manifest를 만나 fail closed했다.
4. runner는 즉시 종료했으며 후속 slot과 EFF 평가를 수행하지 않았다.

## 진단 증거와 처리

Claude가 남긴 Agent별 transcript에는 각 provider request usage가 분리돼 있었다. 실패 표본을 정식 결과로 재사용하지 않는 read-only 진단에서 request별 최종 API block을 집계한 결과는 다음과 같다.

| 역할 | cache creation | cache read |
|---|---:|---:|
| frontend-engineer | 31,366 | 412,536 |
| independent-qa | 21,901 | 149,828 |
| 합계 | 53,267 | 562,364 |
| provider aggregate − outer | 53,267 | 562,364 |

다음 보완은 Agent별 transcript usage를 assignment ID·role·model·transcript digest와 결속하고, delivery specialist는 delivery에, independent-QA는 assurance에 귀속한다. 두 합계가 provider aggregate와 일치하지 않으면 계속 fail closed한다. 사용자 turn·workload·adapter·품질 규칙은 변경하지 않으며 새 Opus cohort는 사용자 재승인 전 실행하지 않는다.
