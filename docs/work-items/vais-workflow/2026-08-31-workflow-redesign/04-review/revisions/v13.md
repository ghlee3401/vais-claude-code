---
schema: vais-phase/v1
work_item: WI-2026-08-31-workflow-redesign
phase: review
revision: 5
status: blocked
based_on:
  - ../../01-plan/main.md
  - ../../02-design/main.md
  - ../../03-do/main.md
verdict: BLOCKED
reviewed_at: 2026-09-06
qa_attempt: 13
---

# Review Attempt 13 — Model-estimated byte target BLOCKED

## 실행 결과

- cohort: `cohort-cb9866091015883baa45f7bd`
- stage root: `/tmp/vais-rev8-formal.wiTfwy`
- 정책: `subscription-no-dollar-cap`, turn timeout 900,000 ms
- 실행 순서: Legacy 1 완료 후 v2 1에서 중단. Legacy 2 이후는 미실행
- EFF-01~08: `NOT_EVALUATED`

| Slot | 결과 | 근거 |
|---|---|---|
| Legacy 1 | quality PASS | 5 user turns, 1,997,999 ms, terminal `completed`, evidence eligible |
| v2 1 | BLOCKED | turn 3 종료 상태가 기대한 `review/waiting-user`가 아니라 `review/active` |

## 원인 판정

Do 구현 handoff, E2E 5/5, independent-QA의 REQ-001~007 / TC-001~006 PASS 판정까지는 완료됐다. 그러나 QA handoff 자동 저장이 실패해 Review Gate를 닫지 못했으므로 formal quality는 BLOCKED다.

1. rev8 assignment는 `x-vais-max-serialized-bytes: 3072`, target 2048, raw JSON-only 계약을 실제 QA agent에 정상 전달했다.
2. QA agent는 raw JSON을 반환했지만 UTF-8 직렬화 결과가 3,086B여서 hard limit을 14B 초과했다.
3. PostToolUse 훅은 정상 fail closed했고 handoff 파일을 쓰지 않았다.
4. main agent는 중단 안내에도 `SendMessage`로 축약본을 요청했지만 해당 결과는 Agent PostToolUse 수집 경로가 아니므로 저장되지 않았다.
5. Work item은 `review/active`, QA assignment는 consumed이지만 handoff 미완료로 남았다.

## 처리

비교 규칙에 따라 즉시 중단했고 실패 표본을 대체하거나 후속 slot을 실행하지 않았다. 3KB 설계 한도는 유지한다. 단순 자연어 target 대신 규모별 `maxLength`·`maxItems`를 QA 전용 output schema에 주입하고, 자동 저장 시 그 발급된 schema까지 검증해 출력의 최대 구조를 결정적으로 제한한다. 보완 후 전체 회귀와 무과금 dry-run을 통과하더라도 새 Opus cohort는 사용자 재승인 전 실행하지 않는다.
